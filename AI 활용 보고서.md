# AI 활용 보고서 — Beat Memories

> **프로젝트**: Beat Memories (콜앤리스폰스 리듬 액션)
> **개발 기간**: 2026-07-24 ~ 2026-07-26 (3일)
> **팀 구성**: 4인 (AI Native 개발자 4인)
> **엔진**: Unity 6000.3.20f1 / URP 2.5D

---

## 1. 요약

본 프로젝트는 **"AI를 코드 생성기가 아니라 문서를 매개로 한 협업 파트너로 쓴다"** 는 원칙으로 진행했다.
사람이 기획 의도를 정하고, AI가 그것을 **문서(SOT, Source of Truth)** 로 고정한 뒤,
그 문서를 기준으로 코드·아트·데이터를 각각 생성하는 방식이다.

3일간 산출물:

| 항목 | 수량 |
|---|---|
| C# 스크립트 | 45개 / 약 7,600줄 |
| ScriptableObject 데이터 에셋 | 50개 |
| AI 생성 캐릭터·포트레잇 아트 | 60장 |
| AI 생성 BGM·효과음 | BGM 1곡(96BPM 루프) + 효과음 3종 |
| UI 스프라이트 | 141장 |
| 아키텍처 문서 변경 이력 | 39건 |
| Git 커밋 | 41개 |

---

## 2. 활용 AI 도구와 역할 분담

여섯 가지 AI를 **역할을 명확히 분리**해 사용했다. 같은 작업을 여러 AI에 중복으로 시키지 않고,
각자의 결과물을 문서로 주고받게 한 것이 핵심이다.

### 2.1 Claude (Claude Code) — 코어 시스템 개발 + 아키텍처 문서화

- **담당**: 리듬 판정 시스템, 전투 로직, 스테이지 진행, 다이얼로그 시스템, 씬 배선
- **산출 문서**: `Assets/BeatMemories-Architecture.MD` (183줄, 변경 이력 39건)
- **특징**:
  - **Unity CLI 연동**: 실행 중인 Unity 에디터에 직접 명령을 보내 씬 오브젝트 생성·컴포넌트 배선·
    스프라이트 임포트 설정을 수행. 사람이 인스펙터를 일일이 클릭하는 과정을 생략.
  - **Skills 활용**: Unity CLI 사용 규약을 스킬 문서로 정의해, AI가 매번 같은 절차
    (컴파일 검증 → 콘솔 확인 → 씬 저장 후 재검증)를 따르도록 표준화.
  - **모든 작업을 아키텍처 문서에 기록**: 무엇을·왜 바꿨는지, 어떤 함정이 있었는지를
    변경 이력으로 남겨 다음 세션과 다른 팀원이 맥락을 이어받을 수 있게 함.

### 2.2 Codex — 시스템 개발 / 타이틀 연출 / 규칙 문서화

- **담당**: 개정된 비트 UI를 비롯한 여러 시스템, 타이틀 씬 연출, UI 흐름 설계
- **산출 문서**: `Markdown/` 문서 세트
  - `title-development.md` (타이틀 개발 기록)
  - `GAME_DESIGN.md`, `COMBAT_RULES.md`, `INPUT_RULES.md`, `UI_FLOW.md` (규칙 명세)
  - `TASKS.md` (작업 큐)
- **특징**: 규칙을 **문서로 먼저 확정**한 뒤 구현에 들어가, 여러 AI가 같은 명세를 참조하도록 함.
- **비고**: `AI_RULES.md`(AI가 지켜야 할 개발 원칙)는 **다른 팀원이 사용한 Codex 인스턴스**의 규약 문서다.
  같은 도구라도 팀원별로 별도의 규약을 두어, 각자의 작업 영역에서 일관성을 유지했다.

### 2.3 NovelAI — 캐릭터 아트 생성

- **담당**: 게임 캐릭터 스프라이트, 대화 포트레잇, 게임 아트 톤 초안
- **산출물**: 캐릭터 아트 27장, 스테이지별 포트레잇 33장
- **후처리**: 생성 이미지는 **포토샵 수작업**(크롭, 누끼/배경 제거)으로 다듬어 게임에 투입하였다.

### 2.4 GPT — 게임 화면 목업 / UI 아트

- **담당**: 게임 화면 목업, UI 아트 전반
- **산출물**: UI 스프라이트 141장 (HP 프레임, 비트 슬롯, 페이즈 배너, 스테이지 클리어 배너,
  다이얼로그 창, 액션 버튼, 타이틀·옵션 화면 등)
- **방식**: 개별 아이콘을 따로 뽑는 대신, **화면 전체 목업을 먼저 생성해 레이아웃을 확정**한 뒤
  그 목업에서 개별 UI 요소를 분해해 생성했다. 목업이 그대로 UI 명세 역할을 해,
  구현 단계에서 배치를 다시 고민할 필요가 줄었다.

### 2.5 NC VARCO Sound / Suno — BGM·효과음

- **담당**: 전투 BGM, 효과음(메트로놈 tick/tack, 스네어 등)
- **리듬 게임 특유의 제약**: 음원이 게임 BPM과 정확히 일치해야 한다. 생성된 BGM을
  **96BPM 루프로 가공**해 `Conductor`의 비트 클록과 동기화했고, 메트로놈 효과음은
  박자마다 DSP 시간에 예약 재생되도록 연결했다.
- **교훈**: 음악 생성 AI는 "분위기 있는 곡"은 빠르게 만들지만, **정확한 BPM·루프 경계**는
  사람이 맞춰야 했다. 리듬 게임에서는 생성 결과물을 그대로 쓸 수 없었다.

---

## 3. 워크플로우

### 3.1 아트: "톤 선정 → 톤 문서화 → 대량 생성" 파이프라인

단순히 필요할 때마다 이미지를 뽑는 대신, **톤을 먼저 고정하고 문서화**하는 단계를 넣었다.

```
1) NovelAI로 아트 톤 탐색        → 사이버펑크 네온 톤 결정
2) 결정된 톤을 문서로 명문화      → Notion "아트 디렉션" 문서
3) GPT로 화면 전체 목업 생성     → UI 배치 / 게임 화면 레이아웃 확정
4) 목업에서 개별 UI 요소 분해    → GPT로 UI 스프라이트 141장
5) NovelAI로 캐릭터 아트 생성    → 스프라이트 27장, 포트레잇 33장
6) 포토샵 후처리                 → 크롭 / 누끼 후 게임에 투입
```

2단계에서 만든 **Notion「아트 디렉션」문서**가 이 파이프라인의 중심이다.
단순한 무드보드가 아니라, **게임 규칙과 직결된 판독성 기준**을 명문화했다.

- **한 줄 방향**: "캐릭터는 읽히게, 화면은 미치게"
- **판정 우선 원칙**: "실루엣이 판정 정보다. 디테일보다 자세가 우선이다"
- **색 규약**: 주인공 = 흰색 + 라임 그린 / 적 = 회청색 + 붉은 발광.
  배경에 청록·마젠타를 쓰되 **주인공 라임과 적 레드는 판정 색으로 보호**한다.
- **검증 기준**: "설명 없이도 처음 보는 사람이 3초 안에 '보고 따라 치는 게임'이라고 이해해야 한다"

이 문서는 아트뿐 아니라 **구현 순서까지 규정**했다.
(단색 배경에서 판정 가독성 검증 → 배경 1장 제작 → 근/중/원경 분리 패럴랙스 →
마디별 조명 상태 → 박자 네온 펄스) 실제 개발도 이 순서를 따랐다.

**효과**: UI 141장(GPT)과 캐릭터 아트 60장(NovelAI)이 **서로 다른 AI로, 다른 시점에** 생성됐음에도
톤이 흐트러지지 않았다. 특히 색 규약을 문서로 고정한 덕분에,
**아트 일관성과 게임 판독성이 동시에 지켜졌다** — 화면이 화려해져도 판정에 필요한
라임/레드 대비는 침범되지 않았다.

### 3.2 코드: "문서를 SOT로 삼는 다중 AI 협업"

문서를 **3계층**으로 나누고, 각 계층의 권위를 명확히 했다.

| 계층 | 위치 | 역할 | 작성 |
|---|---|---|---|
| **기획 SOT** | Notion (`Project Memories`) | 기획서, 아트 디렉션, 레벨 디자인, 박자 구조 | 사람 (AI 보조) |
| **개발 규약** | `Markdown/`, `Assets/prompt.MD` | 전투·입력·UI 규칙, AI 작업 원칙 | Codex |
| **구현 이력** | `Assets/BeatMemories-Architecture.MD` | 무엇을·왜 바꿨는지, 함정과 교훈 | Claude |

4인이 각자 다른 AI와 병렬로 작업하면서도 충돌을 관리할 수 있었던 것은,
**모든 결정이 이 문서 체계에 남아 있었기 때문**이다.

- 기획 변경 시 Notion을 먼저 고치고 → 개발 문서 → 코드 순으로 반영
- AI가 코드를 바꾸면 → 반드시 아키텍처 문서 변경 이력에 "무엇을·왜·어떤 함정" 기록
- 다음 세션의 AI는 그 문서를 읽고 맥락을 복원

**기획 SOT를 교체한 사례**가 실제로 있었다. 초기 GDD 초안(`GGD-Draft.MD`)은 게임을
카드형 턴제로 잘못 해석하고 있었는데, Notion의 「박자게임」 기획이 확정되면서
**구 초안을 명시적으로 폐기하고 SOT를 교체**했다. 이 교체 사실과 정정 내용
(콜앤리스폰스 8박, 판정 축 = 적의 자세)을 아키텍처 문서 최상단에 못박아,
이후 어떤 AI 세션도 폐기된 초안을 참조하지 않도록 했다.

> AI는 주어진 문서를 성실히 따르기 때문에, **틀린 문서가 남아 있으면 틀린 구현이 반복된다.**
> "무엇이 현재 기준인가"를 문서로 명시하는 것이 다중 AI 협업의 핵심이었다.

### 3.3 Unity 에디터 직접 조작

Claude가 Unity CLI를 통해 에디터를 직접 조작하여, 다음 작업을 사람 개입 없이 수행했다.

- 씬 오브젝트 생성 및 계층 구성 (다이얼로그 UI, 암전 오버레이, 액션 버튼 등)
- 컴포넌트 부착 및 인스펙터 필드 배선
- 스프라이트 임포트 설정 일괄 변경 (Multiple → Single, 피벗 조정)
- 프리팹 생성·수정 (씬을 열지 않고 프리팹 소스를 직접 편집)
- 코드 수정 후 컴파일 검증 및 콘솔 에러 확인

---

## 4. AI 활용의 실제 효과

### 4.1 반복 작업 제거

스프라이트 임포트 설정, 컴포넌트 배선 같은 단순 반복 작업을 AI가 처리해
사람은 기획·밸런싱·아트 디렉션에 집중할 수 있었다.

### 4.2 문서가 자동으로 최신 상태 유지

개발과 문서화를 분리하지 않고 **한 번의 작업으로 처리**했다.
일반적으로 문서는 개발 속도를 못 따라가 방치되지만, AI가 코드를 바꿀 때마다
문서 이력을 함께 남기게 하여 3일간 39건의 변경 이력이 실시간으로 축적됐다.

### 4.3 버그 원인 추적

AI가 프로젝트 전체를 빠르게 탐색할 수 있어, 증상만으로 원인을 특정하는 데 효과적이었다.

**사례**: "4·5스테이지의 숨겨진 적 기믹이 작동하지 않는다"는 제보에 대해,
데이터(페이즈 SO의 색상·차폐 설정)와 코드(차폐 판정 로직)가 모두 정상임을 먼저 확인한 뒤,
**연출을 실행할 컴포넌트 자체가 해당 씬에만 누락**되어 있음을 찾아냈다.
(팀이 작업 씬을 옮기는 과정에서 빠진 것)

---

## 5. 한계와 대응

AI 활용에서 실제로 겪은 문제와 대응을 기록한다.

### 5.1 AI가 만든 코드도 병합 과정에서 깨진다

여러 팀원이 각자 AI와 작업한 결과를 병합하는 과정에서, **원격 main 브랜치 자체가
컴파일 불가 상태**가 된 적이 있다. 새 기능의 뷰 스크립트는 병합됐으나,
그것이 참조하는 이벤트 정의는 유실된 채였다.

- **대응**: 병합 후 반드시 컴파일 검증. 정상 동작하던 커밋을 찾아 필요한 정의만 이식.
- **교훈**: AI가 코드를 잘 쓰는 것과, 여러 갈래의 작업이 안전하게 합쳐지는 것은 별개 문제다.

### 5.2 AI는 눈에 보이지 않는 실행 순서 문제에 약하다

다이얼로그가 표시되지 않고 게임이 멈추는 버그가 있었는데, 원인은
**스크립트 실행 순서 경쟁**이었다. 한 매니저가 먼저 실행되어 UI를 켜자마자,
뒤늦게 실행된 다른 컴포넌트가 그것을 다시 꺼버린 것이다.

- **대응**: 초기 상태를 코드가 아닌 **프리팹 데이터로 고정**하도록 구조 변경.
- **교훈**: AI는 코드를 읽어 논리 오류는 잘 잡지만, 런타임 실행 순서처럼
  **실제로 돌려봐야 드러나는 문제**는 사람의 플레이 테스트 제보가 필요했다.

### 5.3 데이터와 코드의 기준이 어긋날 수 있다

여러 AI·여러 사람이 병렬로 작업하다 보니, 한쪽이 판정 기준을 바꿨는데
다른 쪽 시스템은 옛 기준을 보고 있는 상태가 발생했다.

- **대응**: 정기적으로 데이터와 코드의 정합성을 점검.
- **교훈**: 문서화가 이를 상당 부분 막아주지만 완벽하진 않다.
  **"AI가 만들었으니 맞겠지"가 아니라 사람이 결과를 확인해야 한다.**

### 5.4 최종 판단은 사람이 한다

AI에게 게임 디자인 결정을 맡기지 않았다. 다음과 같은 결정은 모두 사람이 내렸다.

- 판정 축을 무엇으로 할 것인가 (적의 자세)
- 차징 기능을 넣을 것인가, 어느 스테이지부터 해금할 것인가
- 스테이지 전환 연출의 범위
- 적이 쓰러진 뒤 남은 공격을 어떻게 처리할 것인가

AI는 선택지와 각각의 트레이드오프를 제시하고, 결정 후 그것을 정확히 구현하는 역할을 맡았다.

---

## 6. 결론

이번 프로젝트에서 AI는 **"빠르게 만들어주는 도구"보다 "맥락을 잃지 않게 해주는 장치"** 로서
더 큰 가치가 있었다.

3일이라는 짧은 기간에 4명이 병렬로 작업하면서도 시스템이 무너지지 않은 이유는,
코드를 빨리 썼기 때문이 아니라 **모든 결정이 문서로 남아 다음 작업자(사람이든 AI든)가
같은 맥락 위에서 이어갈 수 있었기 때문**이다.

동시에, 실행 순서 버그·병합 사고·데이터 정합성 문제처럼
**AI가 스스로 발견하지 못하는 영역이 분명히 존재**했고,
이는 사람의 플레이 테스트와 최종 판단으로 메워야 했다.

---

## 부록: AI 도구별 활용 요약

| 도구 | 역할 | 주요 산출물 |
|---|---|---|
| **Claude (Claude Code)** | 코어 시스템 개발, Unity 에디터 자동화, 구현 이력 문서화 | 전투·리듬·스테이지·다이얼로그 시스템, `BeatMemories-Architecture.MD` |
| **Codex** | 시스템 개발(비트 UI 등), 타이틀 연출, 규칙 문서화 | `Markdown/` 문서 세트, 타이틀 씬, 비트 UI 시스템 |
| **NovelAI** | 캐릭터·포트레잇 아트 생성, 아트 톤 초안 | 캐릭터 27장, 포트레잇 33장 |
| **GPT** | 게임 화면 목업, UI 아트 생성 | UI 스프라이트 141장, 화면 레이아웃 목업 |
| **NC VARCO Sound / Suno** | BGM·효과음 생성 | 전투 BGM(96BPM 루프), 메트로놈·스네어 효과음 |

**AI를 쓰지 않은 영역** (사람이 직접 수행)

- 기획·게임 디자인 결정 (판정 축, 차징 해금 시점, 연출 범위 등)
- 아트 톤 방향 설정 및 「아트 디렉션」 문서 작성
- 생성 이미지 후처리 (포토샵 크롭·누끼)
- 생성 음원의 BPM 정합·루프 경계 가공
- 플레이 테스트 및 밸런싱 판단
